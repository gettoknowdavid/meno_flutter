import 'package:flutter/material.dart';

class LiveNotesTab extends StatelessWidget {
  const LiveNotesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
